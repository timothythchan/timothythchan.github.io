<div class="empty-state-visual" aria-hidden="true">
  <div class="empty-state-orbit"></div>
  <div class="empty-state-orbit empty-state-orbit-delayed"></div>
  <svg
    class="empty-state-icon"
    viewBox="0 0 96 96"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
  >
    <rect
      x="24"
      y="34"
      width="48"
      height="36"
      rx="6"
      class="stroke-hub-subtitle dark:stroke-hub-subtitle-dark"
      stroke-width="2.5"
      fill="none"></rect>
    <path
      d="M32 34V30C32 25.5817 35.5817 22 40 22H56C60.4183 22 64 25.5817 64 30V34"
      class="stroke-hub-subtitle dark:stroke-hub-subtitle-dark"
      stroke-width="2.5"
      stroke-linecap="round"></path>
    <circle
      cx="40"
      cy="52"
      r="2.5"
      class="fill-hub-subtitle dark:fill-hub-subtitle-dark empty-state-dot"></circle>
    <circle
      cx="48"
      cy="52"
      r="2.5"
      class="fill-hub-subtitle dark:fill-hub-subtitle-dark empty-state-dot empty-state-dot-2"
    ></circle>
    <circle
      cx="56"
      cy="52"
      r="2.5"
      class="fill-hub-subtitle dark:fill-hub-subtitle-dark empty-state-dot empty-state-dot-3"
    ></circle>
    <path
      d="M48 18V12M48 12L44 16M48 12L52 16"
      class="stroke-sky-500"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"></path>
  </svg>
</div>

<style>
  .empty-state-visual {
    position: relative;
    width: 6rem;
    height: 6rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .empty-state-icon {
    width: 100%;
    height: 100%;
    animation: empty-bob 2.6s ease-in-out infinite;
  }

  .empty-state-orbit {
    position: absolute;
    inset: 0.25rem;
    border: 1.5px dashed #d4d4d8;
    border-radius: 9999px;
    animation: empty-spin 8s linear infinite;
  }

  :global(.dark) .empty-state-orbit {
    border-color: #3f3f46;
  }

  .empty-state-orbit-delayed {
    inset: 0.75rem;
    animation-direction: reverse;
    animation-duration: 6s;
  }

  .empty-state-dot {
    animation: empty-blink 1.8s ease-in-out infinite;
  }

  .empty-state-dot-2 {
    animation-delay: 0.2s;
  }

  .empty-state-dot-3 {
    animation-delay: 0.4s;
  }

  @keyframes empty-bob {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  @keyframes empty-spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes empty-blink {
    0%,
    100% {
      opacity: 0.35;
      transform: scale(0.9);
    }
    50% {
      opacity: 1;
      transform: scale(1.1);
    }
  }
</style>
